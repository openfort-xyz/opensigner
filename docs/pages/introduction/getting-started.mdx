# Getting Started

After starting the components, e.g. with `make run`, you can start using the Openfort OpenSigner service.

Try either opening the iFrame from the browser (running in port 7054 by default)
or trying our [Postman collection](/apis/postman)!
